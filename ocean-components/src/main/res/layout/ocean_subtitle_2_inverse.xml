<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="text"
            type="String" />

        <variable
            name="gravity"
            type="Integer" />

        <variable
            name="color"
            type="Integer" />

    </data>

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/textOceanSubtitle2Inverse"
        style="@style/Ocean.Subtitle.2.Inverse"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="@{gravity}"
        android:textColor="@{(color != null) ? color : @color/ocean_color_interface_light_down}"
        android:text="@{text}"
        tools:text="Subtitle 2 Inverse" />

</layout>
