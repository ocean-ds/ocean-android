<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="text"
            type="String" />

        <variable
            name="gravity"
            type="Integer" />

        <variable
            name="color"
            type="Integer" />

    </data>

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/textOceanHeading5Inverse"
        style="@style/Ocean.Heading.5.Inverse"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="@{gravity}"
        android:text="@{text}"
        android:textColor="@{(color != null) ? color : @color/ocean_color_interface_light_pure}"
        tools:text="Heading 5 Inverse" />


</layout>
