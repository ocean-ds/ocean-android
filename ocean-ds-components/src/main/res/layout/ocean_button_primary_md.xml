<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="kotlin.jvm.functions.Function0" />

        <import type="kotlin.Unit" />

        <variable
            name="text"
            type="String" />

        <variable
            name="click"
            type="Function0&lt;Unit>" />

        <variable
            name="showProgress"
            type="Boolean" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/buttonOceanPrimaryMedium"
            style="@style/Ocean.Button.MD.Primary"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="@{() -> click.invoke()}"
            android:text="@{showProgress ? @string/ocean_button_empty : text}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Medium" />

        <ProgressBar
            android:id="@+id/progress"
            style="?android:attr/progressBarStyle"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:elevation="5dp"
            android:indeterminateTint="@color/ocean_color_interface_light_pure"
            android:progressTint="@color/ocean_color_interface_light_pure"
            app:layout_constraintBottom_toBottomOf="@+id/buttonOceanPrimaryMedium"
            app:layout_constraintEnd_toEndOf="@+id/buttonOceanPrimaryMedium"
            app:layout_constraintStart_toStartOf="@+id/buttonOceanPrimaryMedium"
            app:layout_constraintTop_toTopOf="@+id/buttonOceanPrimaryMedium"
            app:ocean_visible_or_invisible="@{showProgress}" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>